<p>
  <strong>User:</strong>
  <%= @user.name%>

</p>

<p>Email:
  <%= @user.email%>
</p>

Tests list:
<Table>
  <% @user.test_passages.each do |test_passages| %>
    <tr>
      <td><%= link_to test_passages.test.title, test_path(test_passages.test) %></td>
      <td><%= "Completed" if test_passages.current_question == nil %></td>
    </tr>
  <% end %>
</Table>
<%= link_to "Back", users_path %>
